language: pt
responses:
  intents:
    HassTurnOff:
      default: Desligar {{ slots.name }}
      lights_area: Desligar as luzes {{ slots.area }}
      fans_area: Desligar as ventoinhas {{ slots.area }}
      cover: Fechar {{ slots.name }}
      cover_area: Fechar {{ slots.area }}
