language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligar {{ slots.name }}
      lights_area: Ligar as luzes {{ slots.area }}
      fans_area: Ligar as ventoinhas {{ slots.area }}
      cover: Abrir {{ slots.name }}
      cover_area: Abrir {{ slots.area }}
